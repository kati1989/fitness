<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Felhasználói profil</title>
    <link href="/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">
<div class="container mx-auto p-4">
    <h1 class="text-2xl font-bold mb-4">Felhasználói profil</h1>
    <div class="bg-white p-6 rounded shadow-md">
        <p><strong>Felhasználónév:</strong> {{ user.username }}</p>
        <p><strong>Keresztnév:</strong> {{ user.first_name }}</p>
        <p><strong>Vezetéknév:</strong> {{ user.last_name }}</p>
        <p><strong>Születési dátum:</strong> {{ user.birth_date | formatDate }}</p>
        <div class="flex justify-between mt-4">
            <a href="/users/edit/{{ user.id }}" class="bg-blue-500 text-white px-4 py-2 rounded">Szerkesztés</a>
        </div>
    </div>
</div>
</body>
</html>
